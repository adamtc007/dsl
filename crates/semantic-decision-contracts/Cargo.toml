[package]
name = "semantic-decision-contracts"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
description = "Stable host-neutral semantic decision, evidence, and proposal contracts"

[dependencies]
hex = "0.4"
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
thiserror = "1.0"

[dev-dependencies]
serde_json = "1.0"

[lints]
workspace = true
