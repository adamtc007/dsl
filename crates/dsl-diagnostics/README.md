# dsl-diagnostics

Source-attributed diagnostics (`Span`, `Diagnostic`, `DiagnosticBag`) and the
well-known diagnostic codes shared by the unified DSL atom grammar crates
(`dsl-parser`, `dsl-atoms`, `dsl-ast`). Leaf crate; host-neutral.
