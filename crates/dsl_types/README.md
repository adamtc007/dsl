# dsl_types

Serializable value types shared by the DSL compiler and SemOS layers. This is a
bottom-layer crate and has no dependency on another workspace package.

Types in this crate describe generic structure. Application-specific kinds are
being migrated to validated identifiers declared by semantic packs.
