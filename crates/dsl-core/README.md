# dsl-core

Host-neutral DSL parsing, AST, validation, and compilation. The crate accepts
source/configuration data and produces typed plans and diagnostics without a
database or application runtime dependency.

Application vocabulary belongs in admitted configuration and fixtures, not in
new parser branches or defaults.
