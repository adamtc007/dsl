# sem_os_types

Foundational host-neutral value types used by SemOS crates. It contains no
database integration and depends on no other workspace package.

Application roles, commands, and domain eligibility rules belong in versioned
configuration rather than this crate.
