[package]
name = "dsl-parser"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
description = "S-expression lexer and parser for the unified DSL atom grammar"

[dependencies]
dsl-diagnostics.workspace = true
logos = "0.14"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
