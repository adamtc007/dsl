# sem_os_core

Host-neutral SemOS orchestration primitives, identifiers, principal types, and
storage ports. Concrete persistence and application adapters implement these
ports outside the shared workspace.
