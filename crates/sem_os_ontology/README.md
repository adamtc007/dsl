# sem_os_ontology

Serializable ontology and capability-definition vocabulary used by SemOS. The
crate defines and validates data shapes; it does not enforce policy, perform
database operations, or host application adapters.
