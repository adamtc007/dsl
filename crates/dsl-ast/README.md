# dsl-ast

`AtomBag`: a name-indexed collection of atoms from one source file, each
classified against a caller-supplied `dsl-atoms` catalogue. `AtomParser`
bundles a catalogue with the `dsl-parser` front end for external consumers.
