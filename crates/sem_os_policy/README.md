# sem_os_policy

Host-neutral policy evaluation, governance, projection, and decision machinery
over typed SemOS inputs. Application declarations are configuration inputs;
application persistence and side effects remain in host adapters.

Stable decision contracts will move to a narrower crate while this package
retains compatibility re-exports during the migration window.
