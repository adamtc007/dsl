name: Layering guard

on:
  push:
    branches: [main]
  pull_request:

jobs:
  guard:
    name: dsl-core has no SemOS dependency
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Layering guard
        run: bash scripts/check-layering.sh
